# Build artifacts
/clockify-mcp
coverage.out
dist/
staging/
# Bench profiles captured by `make bench` / `make verify-bench` (D3).
# Local-only — baselines live per workstation, not in the repo.
.bench/

# Editor and tool local state
.idea/
.vscode/
*.swp
*~

# Local-only assistant / IDE state — never committed
.claude/
.serena/
.remember/
.planning/
