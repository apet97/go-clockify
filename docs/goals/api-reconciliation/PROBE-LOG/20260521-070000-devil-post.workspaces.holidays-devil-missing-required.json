{
  "endpoint_id": "createHoliday",
  "method": "POST",
  "path": "/workspaces/{workspaceId}/holidays",
  "probe_kind": "devil-missing-required",
  "timestamp_utc": "2026-05-21T07:00:00Z",
  "truth_yaml_said": {
    "x-verification-status": "LIVE-VERIFIED",
    "x-evidence": ["PROBE-LOG/20260521-020000-post.workspaces.holidays-happy-path.json"],
    "x-truth-iter": "iter18",
    "x-notes": "Actual create + delete. dry_run not supported. Response has date_period:{start_date,end_date} object shape. Requires name + start_date.",
    "required_fields": ["name", "start_date"],
    "optional_fields": ["end_date", "occurs_annually", "user_ids"],
    "response_shape": "object with {id, name, workspaceId, date_period:{start_date, end_date}, occursAnnually}"
  },
  "live_probe_result": {
    "description": "Called clockify_holidays_create with only name, omitting required start_date",
    "call": {
      "name": "DEVIL-PROBE-MISSING-REQUIRED-20260521",
      "start_date": "(omitted)"
    },
    "error": "MCP error -32602: invalid params at /start_date: missing required property",
    "error_source": "MCP schema validation (client-side, before any upstream call)",
    "no_upstream_call_made": true
  },
  "verdict": "CONFIRMED",
  "demotion_reason": null,
  "notes": "TRUTH.yaml correctly documents start_date as required. The MCP tool's own JSON Schema enforces this locally (JSON-RPC -32602) before reaching Clockify. This confirms the required-field contract is enforced. No entity was created — safe probe. Additional observation: the MCP descriptor also lists user_ids as optional but notes upstream rejects holidays with no assignment — TRUTH.yaml does not capture user_group_ids as a separate optional field even though the clockify_holidays_create MCP tool descriptor accepts it. This is a minor gap in TRUTH.yaml (user_group_ids undocumented in requestBody) but does not affect the create contract."
}
