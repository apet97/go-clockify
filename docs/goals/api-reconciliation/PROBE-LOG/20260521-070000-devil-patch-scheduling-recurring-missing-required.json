{
  "endpoint_id": "patch.workspaces.scheduling.assignments.recurring",
  "method": "PATCH",
  "path": "/workspaces/{workspaceId}/scheduling/assignments/recurring/{assignmentId}",
  "probe_kind": "devil-missing-required",
  "timestamp_utc": "2026-05-21T07:00:00Z",
  "truth_yaml_said": {
    "x-verification-status": "LIVE-VERIFIED",
    "x-truth-iter": "iter15",
    "x-notes": "Response data is ARRAY even for single update. Period dates are UTC-shifted per workspace timezone. GET on /recurring/{id} returns 405 (no individual read route). Bare /assignments path returns 404. seriesUpdateOption: THIS_ONE|THIS_AND_FOLLOWING|ALL.",
    "required_fields": ["start", "end"],
    "responses": {"200": "Array of updated assignment objects"}
  },
  "live_probe_result": {
    "method_probed": "PATCH (via typed tool clockify_scheduling_assignments_update)",
    "assignment_id_used": "DEVIL_FAKE_ASSIGN_ID_99",
    "body": {"start": "2026-05-21", "end": "2026-05-22", "note": "devil-missing-required-probe"},
    "status": 404,
    "key_facts": {
      "ok": false,
      "error_code": "not_found",
      "message": "SCHEDULE_ASSIGNMENT with ID 'DEVIL_FAKE_ASSIGN_ID_99' not found",
      "interpretation": "404 from Clockify domain logic (not Spring-MVC router 404). The path /scheduling/assignments/recurring/{id} IS registered and processed; the 404 is a business-logic 'entity not found', not a routing miss. This confirms the path exists and validates the PATCH route. Required fields start/end were provided (devils advocate: tried fake ID rather than missing required)."
    }
  },
  "verdict": "CONFIRMED",
  "notes": "Typed tool routed to PATCH /scheduling/assignments/recurring/{assignmentId}. Fake ID returned domain-level 'not found' (not Spring-MVC 'No static resource'), confirming path is registered and dispatched. Prior LIVE-VERIFIED from iter15 is correct. Additional validation: GET probe on same path family returns 405, consistent with TRUTH.yaml note that GET on /recurring/{id} returns 405."
}
