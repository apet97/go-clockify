{
  "endpoint_id": "post.workspaces.projects.from-template",
  "method": "POST",
  "path": "/workspaces/{workspaceId}/projects/from-template",
  "probe_kind": "path-existence",
  "timestamp_utc": "2026-05-21T02:00:00Z",
  "request": {
    "method": "GET via clockify_api_get (path-existence probe)",
    "path": "GET /workspaces/[REDACTED-WORKSPACE]/projects/from-template",
    "query": {},
    "headers_redacted": {"X-Api-Key": "[REDACTED]"},
    "body": null
  },
  "response": {
    "status": "400/404-equivalent",
    "headers_redacted": {},
    "body_sample": {
      "ok": false,
      "action": "clockify_api_get",
      "error": {
        "code": "invalid_request",
        "message": "project not found in this workspace"
      }
    }
  },
  "facts_observed": {
    "clockify_routing_behavior": "Clockify treated 'from-template' as a project ID slug under /projects/{projectId}, not as a sub-route /projects/from-template",
    "response_meaning": "No dedicated route registered at GET /workspaces/{wId}/projects/from-template; Clockify's router consumed 'from-template' as a project ID and returned 'project not found'",
    "spring_mvc_rule": "Unlike a 405 (path exists, wrong method), this 'project not found' error means the /projects/from-template route is NOT registered as a standalone path — it routes through /projects/{projectId} and fails ID lookup",
    "post_attempt": "POST via clockify_api_request blocked: CLOCKIFY_ENABLE_RAW_WRITES=false (raw writes disabled)",
    "mcp_tool_exposure": "clockify_create_project_from_template exists in Go source (project_admin.go line 64) with dry_run support and template_project_id param but is NOT exposed in the live MCP registry (not present in deferred tools list)",
    "templates_available": "4 templates found in workspace via clockify_projects_templates_list",
    "template_id_used_for_attempt": "<redacted-id:700c44d7aa> (name: '144', archived:true)",
    "both_specs_agree": "Both OpenAPI specs list this endpoint — but live routing suggests Clockify may route POST /projects/from-template differently than a standalone path"
  },
  "notes": "UNCONFIRMED-AGREE. The GET probe returned 'project not found' (not 405), indicating Clockify does NOT have a dedicated route at /projects/from-template — the segment is consumed as a project ID. POST cannot be tested with raw writes disabled. The typed MCP tool (clockify_create_project_from_template) exists in source with dry_run but is not exposed in the live MCP registry. Both specs agree the endpoint exists, but live routing behavior is ambiguous. If Clockify handles POST /projects/from-template under /projects/{projectId} routing (treating it as project ID 'from-template'), the POST would fail with 'project not found'. This needs re-probing with CLOCKIFY_ENABLE_RAW_WRITES=true."
}
