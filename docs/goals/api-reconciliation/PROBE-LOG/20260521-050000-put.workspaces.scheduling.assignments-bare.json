{
  "probe_id": "put.workspaces.scheduling.assignments",
  "ts": "2026-05-21T05:00:00Z",
  "iter": "iter21",
  "batch": "C",
  "method": "PUT",
  "path_template": "/workspaces/{workspaceId}/scheduling/assignments/{assignmentId}",
  "path_probed": "/workspaces/[REDACTED]/scheduling/assignments/<redacted-id:cb0cb27178>",
  "source_file": "internal/tools/scheduling.go:521 (does NOT use this path)",
  "mcp_tool": "clockify_scheduling_assignments_update (uses /recurring/ path, not bare)",
  "probe_method": "GET_path_probe",
  "steps": [
    {
      "step": 1,
      "action": "GET /scheduling/assignments/{assignmentId} (bare path)",
      "result": "404 Spring 'No static resource v1/workspaces/.../scheduling/assignments/<redacted-id:cb0cb27178>.'",
      "interpretation": "Bare /scheduling/assignments/{id} path is NOT registered on the live server"
    },
    {
      "step": 2,
      "action": "Code inspection of scheduling.go updateAssignment()",
      "result": "Line 521: paths.Workspace(wsID, 'scheduling', 'assignments', 'recurring', aID) - uses PATCH on /recurring/ path",
      "interpretation": "Our implementation does NOT use this bare PUT path. It uses PATCH /recurring/{id} instead."
    },
    {
      "step": 3,
      "action": "Code inspection of raw_allowlist_gen.go",
      "result": "Line 122: 'PUT /workspaces/{workspaceId}/scheduling/assignments/{assignmentId}': true - allowlist entry exists",
      "interpretation": "Raw allowlist registers this path but it does not exist live and is not used by any typed tool"
    }
  ],
  "verdict": "LIVE-OVERRIDE",
  "notes": "GET probe returns Spring 404 'No static resource' confirming the bare /scheduling/assignments/{id} path does not exist on the live Clockify API. This matches the CLAUDE.md note: 'the bare /scheduling/assignments/{id} route returns a 404 No static resource'. Our clockify_scheduling_assignments_update tool correctly uses PATCH /scheduling/assignments/recurring/{id} instead. The raw allowlist entry for PUT /scheduling/assignments/{id} is stale and should be removed.",
  "redacted_fields": ["workspaceId", "X-Api-Key"]
}
