apiVersion: v1
kind: Service
metadata:
  name: clockify-mcp
  namespace: clockify-mcp
  labels:
    app.kubernetes.io/name: clockify-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: clockify-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
