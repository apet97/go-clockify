apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "clockify-mcp.fullname" . }}-config
  labels:
    {{- include "clockify-mcp.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
